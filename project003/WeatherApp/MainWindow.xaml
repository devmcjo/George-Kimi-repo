<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WeatherApp - 5일 날씨 예보" Height="650" Width="900"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid Background="#FF1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 헤더 -->
        <Border Grid.Row="0" Background="#FF2D2D2D" Padding="20" Margin="10">
            <StackPanel>
                <TextBlock Text="🌤️ WeatherApp" FontSize="28" FontWeight="Bold" 
                          Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="5일 날씨 예보" FontSize="14" Foreground="#FFAAAAAA" 
                          HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- 검색 영역 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0" x:Name="CityTextBox" Height="40" FontSize="16" 
                    Padding="10,5" Background="#FF2D2D2D" Foreground="White" 
                    BorderBrush="#FF555555" BorderThickness="1"
                    Text="Seoul" VerticalContentAlignment="Center"/>
            
            <Button Grid.Column="1" x:Name="SearchButton" Content="🔍 검색" Width="100" Height="40" 
                   Margin="10,0,0,0" FontSize="14" Click="SearchButton_Click"
                   Background="#FF4CAF50" Foreground="White" BorderThickness="0"/>
            
            <Button Grid.Column="2" x:Name="SettingsButton" Content="⚙️ 설정" Width="80" Height="40" 
                   Margin="10,0,0,0" FontSize="14" Click="SettingsButton_Click"
                   Background="#FF555555" Foreground="White" BorderThickness="0"/>
        </Grid>
        
        <!-- 날씨 표시 영역 -->
        <ScrollViewer Grid.Row="2" Margin="10" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="WeatherPanel">
                <!-- 로딩 표시 -->
                <TextBlock x:Name="LoadingText" Text="도시명을 입력하고 검색 버튼을 클릭하세요" 
                          FontSize="16" Foreground="#FFAAAAAA" HorizontalAlignment="Center" 
                          Margin="0,50,0,0"/>
                
                <!-- 오류 메시지 -->
                <TextBlock x:Name="ErrorText" Text="" FontSize="14" Foreground="#FFFF6B6B" 
                          HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                
                <!-- 날씨 카드 컨테이너 -->
                <WrapPanel x:Name="WeatherCards" Margin="0,20,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
